var riskorn = 5;
var ruta1 = 1;
var rutatot = 1;
    while (ruta1 < riskorn)
    {
        ruta1 = ruta1 * 2;
        rutatot = rutatot + 1;
    }
    Console.WriteLine("Du hamnade på ruta: " + rutatot);